<h2>Articles list</h2>
<% @articles.each do |a| %>
    <p>
        <%= a.title %></a><br/>
        <%= a.text %><br/>
        <%= link_to 'Show', article_path(a) %>      <%= link_to 'Edit', edit_article_path(a) %>
    </p>
<% end %>
